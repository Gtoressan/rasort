// TODO: add meaningful names to fields
pub struct Config {
    pub UNSORTED_VECTOR: bool,
    pub UNSORTED_VECTOR_COLUMNS: u32,
    pub SORTED_VECTOR: bool,
    pub SORTED_VECTOR_COLUMNS: u32,
    pub COMPARISONS: bool,
    pub MEMORY_ACCESSES: bool,
    pub TIME: bool,
    pub VECTOR_LEN: bool,
}
